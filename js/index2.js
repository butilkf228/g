let elementWidth = '50px';

const parsedElementWidth = Number.parseInt(elementWidth);
// console.log(parsedElementWidth)

let elementHeight = '200.74px';
const result = Number.parseFloat(elementHeight);

const salary = 1300.16472;
const number = salary.toFixed(1);

let quantity = '30'
let value = 'wwww';

// console.log(Number.isNaN(value))

const base = 2;
const power = 5;

const minNumber = Math.pow(base, power);
// console.log(base ** power);

const num1 = prompt('введіть число 1');
const num2 = prompt('введіть число 2');

const math = Math.pow(num1, num2);
// console.log(math);

const randomNumber = Mathrandom = Math.round(Math.random() * (50 - 15) + 15)
console.log(randomNumber)